<div class="hero-unit">
  <h1>Oh, hey.  You're back.</h1>
  <h3>You can <%= link_to "view this poll", poll_path(@poll.id)  %> or <%= link_to "share this poll", new_response_path(@poll.name) %></h3>

  <%= render :partial => 'polls/form', :locals => {:poll => @poll} %>

  <%= form_for @poll.questions.new do |f| %>
    <div class="form-inline">
	  <%= f.hidden_field :poll_id, :value => @poll.id %>
      <%= f.text_field :text, :placeholder => 'What do you want to ask?' %>  <%= f.submit :class => 'btn btn-primary btn-large' %>
    </div>
  <% end %>

  <% @poll.questions.each do |question| %>
	<% if !question.text.nil? %>
	  <%= render :partial => 'questions/display', :locals => {:question => question, :poll => @poll }%>
	<% end %>
  <% end %>
</div>

